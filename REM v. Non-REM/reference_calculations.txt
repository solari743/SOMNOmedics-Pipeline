CoV = np.std(x) / np.mean(x)

Skewness = skew(x)  # from scipy.stats import skew

ZCR = np.mean(np.diff(np.sign(x - np.mean(x))) != 0)

Velocity = np.sqrt(np.sum(np.diff(acc_data, axis=0)**2, axis=1))

Magnitude = np.sqrt(np.sum(acc_data**2, axis=1))

Bowl_Skew = (np.mean(q[0] + q[3]) / np.mean(q[1] + q[2])) - 1